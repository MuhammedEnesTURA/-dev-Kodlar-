//Bir dizi içindeki en büyük sayıyı en hızlı bulmak için en iyi algoritma lineer aramadır ve en hızlı şekilde en büyük sayıyı bulmamızı sağlar.
import random
import time

def max_element(arr):
    en_buyuk = arr[0]
    for i in range(1, len(arr)):
        if arr[i] > max_num:
            en_buyuk = arr[i]
    return en_buyuk

arr = [random.randint(1, 100000) for i in range(10000)]

başlama_zamani = time.time()
en_buyuk = max_element(arr)
sure = time.time()

print("En büyük sayı: ", en_buyuk)
print("Süre: ", sure – başlama_zamani, " saniye")

//En büyük sayı yaklaşık 0.000999 saniyede bulunur.







//BruteForce, her elemanı diğer elemanlarla karşılaştırdığından, O(n^2) zaman karmaşıklığına sahiptir ve büyük veri setleri için yavaş çalışabilir.

import random
import time

def max_element_bruteforce(arr):
    en_buyuk = arr[0]
    for i in range(len(arr)):
        for j in range(len(arr)):
            if arr[i] > arr[j]:
                en_buyuk = arr[i]
    return en_buyuk

arr = [random.randint(1, 100000) for i in range(10000)]

başlama_Zamani = time.time()
en_buyuk = max_element_bruteforce(arr)
sure = time.time()

print("En büyük sayı: ", en_buyuk)
print("Süre: ", sure – başlama_zamani, " saniye")

//En büyük sayı yaklaşık 9.908 saniyede bulunur.

//BruteForce algoritması lineer zamanlı algoritmadan çok daha yavaş çalışır ve büyük veri setleri için uygun değildir.
